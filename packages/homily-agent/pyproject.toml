[project]
name = "homily-agent"
version = "0.1.0"
description = "Homily agent for homily content generation and spiritual guidance"
readme = "README.md"
authors = [{name = "Prete-a-porter Contributors", email = "contributors@preteaporter.local"}]
license = {text = "AGPL-3.0-only"}
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
    "langchain-core>=0.3.0",
    "langgraph>=0.3.0",
    "numpy>=1.24.0,<2",
    "fastapi>=0.109.0",
    "httpx>=0.25.0",
    "uvicorn>=0.27.0",
    "sse-starlette>=2.0.0",
    "a2a-protocol",
    "chromadb>=0.5.0,<0.6.0",
]

[tool.uv.sources]
a2a-protocol = { path = "../a2a-protocol", editable = true }

[project.optional-dependencies]
ml = [
    "sentence-transformers>=2.2.0,<2.7.0",
    "torch>=2.5.0,<3.0.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "pymupdf>=1.24.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--verbose",
]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.coverage.run]
source = ["src"]
branch = true
omit = [
    "*/tests/*",
    "*/test_*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]
[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
